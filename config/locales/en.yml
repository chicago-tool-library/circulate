en:
  date:
    formats:
      default:
        "%m/%d/%Y"
      with_weekday:
        "%A, %B %d, %Y"

  time:
    formats:
      default:
        "%a, %b %-d, %Y at %r"
      date:
        "%b %-d, %Y"
      short:
        "%B %d"
      hour:
        "%l %P"
