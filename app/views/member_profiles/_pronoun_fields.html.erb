<label>Pronouns</label>
<br>
<% Member.pronoun_list.each do |pronoun| %>
  <%= label_tag do %>
    <%= check_box_tag 'member[pronouns][]', pronoun , member.pronouns.include?(pronoun)%> <%= pronoun %>
  <% end %>
  <br>
<% end %>
<%= text_field_tag "member[pronouns][]", nil, value: member.pronouns.reject { |p| Member.pronoun_list.include?(p) } , class: "form-input", placeholder: "Enter your own (optional)" %>
