<%= form_with(model: [:admin, user], builder: SpectreFormBuilder) do |form| %>
  <%= form.errors %>

  <%= form.text_field :email %>
  <%= form.select :role, User.roles %>

  <%= form.actions do %>
    <%= form.submit %>
  <% end %>
<% end %>
