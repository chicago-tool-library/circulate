<%= form_with(model: reservation, url: [:admin, reservation, :review], builder: SpectreFormBuilder) do |form| %>
  <%= form.text_area :notes %>
  <%= form.submit "Approve", name: "reservation[event]", value: "approve" %>
  <%= form.button "Reject", name: "reservation[event]", value: "reject" %>
<% end %>
