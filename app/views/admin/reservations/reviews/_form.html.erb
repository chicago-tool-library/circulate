<%= form_with(model: reservation, url: [:admin, reservation, :review], builder: SpectreFormBuilder) do |form| %>
  <%= form.select :status, options_for_select(reservation_status_options, reservation.status), prompt: true %>
  <%= form.text_area :notes %>
  <%= form.submit %>
<% end %>
