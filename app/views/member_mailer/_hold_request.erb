<% if hold_request.event_id? %>
  <p>You have requested the following items be held for pickup on <strong><%= hold_date(hold_request.event) %></strong>.</p>
<% else %>
  <p>You have requested the following items. <strong>We'll email you when they are ready and you can then schedule a time to pick them up.</strong></p>
<% end %>

<ul>
<% hold_request.items.each do |item| %>
  <li>
    <strong><%= full_item_number(item) %> - <%= item.name %></strong>
  </li>
<% end %>
</ul>
<p>Please keep an eye on your email, as we may need to contact you about your reservation.</p>