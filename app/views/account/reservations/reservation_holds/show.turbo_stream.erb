<turbo-stream action="replace" target="<%= dom_id(@reservation_hold) %>">
  <template>
    <%= render partial: "account/reservations/reservation_holds/reservation_hold", formats: [:html], locals: {reservation: @reservation, reservation_hold: @reservation_hold} %>
  </template>
</turbo-stream>
